{
  "private": "true",
  "name": "realm-monorepo",
  "workspaces": [
    "app",
    "playground/ui"
  ],
  "scripts": {
    "start": "yarn workspace @holium/realm start",
    "realm:install": "yarn workspace @holium/realm install",
    "realm:add": "yarn workspace @holium/realm add",
    "realm:build": "yarn workspace @holium/realm build",
    "realm:package": "yarn workspace @holium/realm package"
  }
}
