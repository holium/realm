import { FC } from 'react';

interface EthTransactionProps {}

export const EthTransaction: FC<EthTransactionProps> = (
  props: EthTransactionProps
) => {
  // return
  return <div>Eth Transaction</div>;
};
