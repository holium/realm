import { FC } from 'react';

interface WalletSettingsProps {}

export const WalletSettings: FC<WalletSettingsProps> = (
  props: WalletSettingsProps
) => {
  // return
  return <div>Wallet settings</div>;
};
