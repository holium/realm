import { FC } from 'react';
import { Flex } from 'renderer/components';

interface RoomChatProps {}

export const RoomChat: FC<RoomChatProps> = (props: RoomChatProps) => {
  return <Flex>chat</Flex>;
};
