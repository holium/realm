import { FC } from 'react';
import { Flex } from 'renderer/components';

interface RoomInfoProps {}

export const RoomInfo: FC<RoomInfoProps> = (props: RoomInfoProps) => {
  return <Flex>Info</Flex>;
};
